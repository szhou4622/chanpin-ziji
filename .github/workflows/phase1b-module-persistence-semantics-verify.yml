name: Phase1B module persistence semantics verify

on:
  push:
    branches:
      - phase1b/module-persistence-failure-semantics
    paths:
      - '.github/workflows/phase1b-module-persistence-semantics-verify.yml'

permissions:
  contents: write

jobs:
  patch-and-verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: phase1b/module-persistence-failure-semantics
          fetch-depth: 0
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: product-operation-report-app/package-lock.json
      - name: Apply module persistence boundary and verify
        shell: bash
        run: |
          set -euo pipefail
          python .github/scripts/phase1b-module-persistence-semantics.py
          cd product-operation-report-app
          npm ci
          npm run typecheck
          npm run test:unit
          cd ..
          git diff --check
      - name: Commit verified persistence boundary
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add product-operation-report-app/src/renderer/src/store.ts
          git commit -m 'fix: keep completed modules successful when snapshot save fails'
          git push origin HEAD:phase1b/module-persistence-failure-semantics
